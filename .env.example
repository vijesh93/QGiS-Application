# Proxy Settings
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=

# Database / App settings
# Set these before running docker compose so services pick them up.
DB_USER=
DB_PASSWORD=
POSTGRES_DB=
DB_HOST_PORT=
BACKEND_HOST_PORT=
FRONTEND_HOST_PORT=
TILES_HOST_PORT=

# For Javascript frontend:
VITE_API_URL=